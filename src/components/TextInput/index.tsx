import React from 'react';
import { TextInput as RNTextInput, TextInputProps, View } from 'react-native';

const TextInput: React.FC<TextInputProps & { label?: string }> = (props) => {
  return <View>
	</View>;
};

export default TextInput;
